# 数组
## 双指针技巧秒杀七道数组题目
+ 双指针分为两类：左右指针和快慢指针
  + 左右指针，两个指针相向/相背而行
  + 快慢指针，两个指针同向而行，一快一慢
### 快慢指针技巧
1. 原地修改
   + [Leecode] 26. Remove Duplicates from Sorted Array
   + slow fast 双指针，当fast和slow不同的时候直接进行替换
   + [Leecode] 83. Remove Duplicates from Sorted List
   + 解法同上
     > 没有head的链表结点，如果是有gc的语言会自动回收，没有的话需要手动回收
   + [Leecode] 27. Remove Element
   + 对数组中的指定元素进行「原地删除」
     > 和 Lc26 去重的差异，slow++的位置不同 → 在于是否需要保留第一个元素
   + [Leecode] 283. Move Zeroes
   + 解法1 双指针，fast负责遍历全array，slow保证第一个非0的位置
   + 解法2 覆写法，先删除到末尾，再将结尾全转化为0




















2. [滑动窗口](#滑动窗口算法核心代码模板)

### 左右指针的常用算法
1. 二分查找
``` python
def binarySearch(nums: List[int], target: int) -> int:
    # 一左一右两个指针相向而行
    left, right = 0, len(nums) - 1
    while left <= right:
        mid = (right + left) // 2
        if nums[mid] == target:
            return mid
        elif nums[mid] < target:
            left = mid + 1
        elif nums[mid] > target:
            right = mid - 1
    return -1
```
2. n 数之和
+ [Leecode] 167. Two Sum II - Input Array Is Sorted
+ 数组有序，优先考虑双指针

3. 反转数组
+ [Leecode] 344. Reverse String
+ 双指针交换

4. 回文串判断
```python
def isPalindrome(s: str) -> bool:
    # 一左一右两个指针相向而行
    left, right = 0, len(s) - 1
    while left < right:
        if s[left] != s[right]:
            return False
        left += 1
        right -= 1
    return True
```
+ [Leecode] 5. Longest Palindromic Substring
+ 长度可能为奇偶，同时从中心向两端进行扩散
+ 遍历时，同时找以s[i]为中心和以(s[i],s[i+1])为中心的回文串
  > python找最值：max(a, b, c, key = len)


## 滑动窗口算法核心代码模板
+ （穷举）暴力for循环解法，双层for循环，复杂度为n^2
+ （非穷举）滑动窗口的复杂度是n，区别在于指针不回退
+ 辨别的关键问题
  1. 什么时候扩大窗口？
  2. 什么时候缩小窗口？
  3. 什么时候更新结果？


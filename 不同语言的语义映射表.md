### 方法定义
```C#
private int Function(int x) {
    return 100;
}

```
``` python
# 自动定义类型
def function(x):
    return 100

# 指定类型
def function(x: int) -> int:
    return 100

```
### 访问修饰符 AccessLevel
+ C# 
public / protected / private / internal ...

+ Python - Not enforced
name / _name / __name / 无